---
import { formatDate } from 'src/date.js'
const { date } = Astro.props
---

<time aria-label={formatDate(date, 'LLLL do, yyyy')} datetime={date} class="block pb-fl-3xs text-fl-xs font-sans font-bold border-b-4 border-current">
  {formatDate(date)}
</time>
